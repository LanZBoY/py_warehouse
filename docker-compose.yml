services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: wentee
      POSTGRES_PASSWORD: jp4wu61215
      POSTGRES_DB: warehouse
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:latest
    ports:
      - "6379:6379"

volumes:
  postgres_data:
